<h1>Details</h1>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit this task", edit_path %>
<%= link_to "Remove this task",
  task_path(@task.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
